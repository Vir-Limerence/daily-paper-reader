---
title: "MO-CAPO: Multi-Objective Cost-Aware Prompt Optimization"
title_zh: "MO-CAPO: 多目标成本感知提示优化"
authors: "Jan Büssing, Moritz Schlager, Timo Heiß, Tom Zehle, Matthias Feurer"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.18869v1"
tags: ["query:prompt-opt"]
score: 9.0
evidence: 多目标自动提示优化，联合优化性能和推理成本并分配预算
tldr: LLMs性能高度依赖提示设计，现有优化方法多忽略推理成本等多目标权衡。本文提出MO-CAPO，联合优化性能与推理成本，采用预算分配提升优化效率，并设计面向部署的成本建模。实验表明，MO-CAPO在多个任务和模型上发现多样帕累托前沿，噪声R2指标8/12案例优于NSGA-II，且低成本下性能竞争。该方法为实践者提供高效、灵活的性能-成本权衡选择。
source: arxiv
selection_source: fresh_fetch
motivation: 现有提示优化仅重性能，忽略推理成本与优化效率，亟需多目标高效方法。
method: 提出MO-CAPO，联合优化性能与成本，采用预算分配提升效率，并设计面向部署的成本目标。
result: 在4任务3模型上，MO-CAPO发现的帕累托前沿噪声R2在8/12案例中优于NSGA-II，且低成本下性能竞争。
conclusion: MO-CAPO实现高效多目标提示优化，为实践者提供灵活的性能-成本权衡，且考虑泛化鲁棒性评估。
---

## 摘要
大型语言模型（LLMs）在广泛任务中表现出色，但对提示设计高度敏感，这催生了对自动提示优化的需求。现有方法主要关注性能，忽略了推理成本或延迟等竞争目标。同时，多目标提示优化的现有工作依赖于现成的NSGA-II，忽视了优化效率。为此，我们提出了MO-CAPO，一种新颖的多目标提示优化算法，该算法在联合优化性能和推理成本的同时，利用预算分配实现成本高效的优化。我们进一步提出了一个面向部署的成本目标，以捕捉LLM推理的完整计算特征。我们在四个任务和三个LLMs上评估了我们的方法，并将其与基于NSGA-II的多目标方法以及最先进的单目标提示优化器进行了比较。结果表明，MO-CAPO能够持续识别出强大、鲁棒且多样化的帕累托前沿近似集，同时保持成本效率。在噪声R2指标上，它在12个案例中的8个上优于NSGA-II基线，并且通常以显著更低的预算实现了具有竞争力的性能。发现的解集涵盖了单目标优化器所忽略的多样化性能-成本权衡，而性能最佳的候选者仍然与单目标解具有竞争力。此外，我们首次对多目标机器学习实验进行了评估，通过噪声R2和近似差距来考察泛化性和鲁棒性，从而更真实地评估解的质量。MO-CAPO使实践者能够从一个高效发现的多提示集中进行选择，这些提示提供了性能与成本之间的不同权衡。

## Abstract
Large language models (LLMs) achieve strong performance across a wide range of tasks but are highly sensitive to prompt design, motivating the need for automatic prompt optimization. Existing methods predominantly focus on performance alone, ignoring competing objectives such as inference cost or latency. At the same time, existing work on multi-objective prompt optimization relies on off-the-shelf NSGA-II, ignoring optimization efficiency. As a remedy, we introduce MO-CAPO, a novel multi-objective prompt optimization algorithm that jointly optimizes performance and inference cost while leveraging budget allocation for cost-efficient optimization. We further propose a deployment-oriented cost objective that captures the full computational profile of LLM inference. We evaluate our approach across four tasks and three LLMs and compare it to an NSGA-II-based multi-objective method and state-of-the-art single-objective prompt optimizers. Results show that MO-CAPO consistently identifies strong, robust, and diverse Pareto front approximations while maintaining cost-efficiency. It outperforms the NSGA-II baseline on 8 out of 12 cases in terms of the noisy R2 metric and achieves competitive performances often already at a considerably lower budget. The discovered solution sets span diverse performance-cost trade-offs that are omitted by single-objective optimizers, yet the top-performance candidates remain competitive with single-objective solutions. Additionally, we conduct the first evaluation of multi-objective machine learning experiments that considers generalization and robustness through noisy R2 and approximation gap, enabling a more realistic assessment of solution quality. MO-CAPO enables practitioners to select from an efficiently discovered set of multiple prompts offering different trade-offs between performance and cost.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
*   **研究动机**：大型语言模型（LLM）的性能高度依赖于提示（prompt）的设计，但手动优化提示繁琐且不可靠。现有自动提示优化方法大多只关注单一的性能指标，忽略了实际部署中同样重要的**推理成本或延迟**等多重竞争目标。
*   **核心问题**：实践者需要一种高效的**多目标优化方法**，能够自动发现一组在**性能与推理成本之间取得不同权衡**的提示（即帕累托前沿）。然而，现有的多目标提示优化方法不仅成本函数过于简单（如仅计算提示长度），还依赖通用的NSGA-II算法，忽视了提示评估本身高昂的计算成本和优化效率。
*   **整体含义**：本文提出MO-CAPO算法，旨在高效地联合优化提示的性能与推理成本，为用户提供一个成本高效的优化过程和一组可供选择的、多样的“性能-成本”权衡方案。

### 2. 论文提出的方法论
MO-CAPO的核心是扩展了CAPO（一种单目标进化提示优化算法）以进行真正的多目标优化，并集成了一种高效的预算分配机制。其关键技术细节和流程如下：
*   **核心思想与问题建模**：
    *   将提示优化建模为一个**双目标黑箱优化问题**：目标一是最大化性能（如准确率），目标二是最小化推理成本。
    *   采用**后验式方法**，旨在返回一个近似的帕累托最优解集，而非预设权重的单一解。
*   **关键技术细节**：
    *   **面向部署的成本目标**：提出一个更真实的成本函数，不仅考虑提示长度，还**对输入和输出令牌（token）分别加权**，以反映现代LLM推理中（如思维链）输出令牌成为主要成本驱动者的现实。公式为：`成本 = 输入权重 × 输入令牌数 + 输出权重 × 输出令牌数`。
    *   **基于预算分配的增强机制（Intensification）**：这是算法效率的关键。它源自多目标自动化算法配置（MO-AAC）的思想，通过**渐进式评估**来节省预算。一个“挑战者”提示不会立即在所有数据上被评估，而是先在部分数据块上评估，然后与其在目标空间上最近的现有最优候选者（称为锚点）进行比较。如果该挑战者在少量数据上就被锚点“支配”（即性能和成本均不占优），则提前终止其评估，节省大量成本。这避免了把算力浪费在明显较差的提示上。
*   **算法流程**：
    1.  **初始化**：创建包含指令和少量示例的初始提示种群，并在一个随机数据块上进行初步评估，形成初始的非支配解集（锚点集）。
    2.  **进化循环**：
        *   **父代选择**：通过二元锦标赛选择，偏好非支配解和多样性好的解。
        *   **生成后代**：利用大模型（作为元模型）对父代提示进行语义交叉和变异，产生新的候选提示。
        *   **增强与筛选**：新候选提示进入增强机制，通过多轮渐进式评估与锚点比较，若表现不佳则提前淘汰（但仍保留为遗传素材）；若能“存活”至全量评估，则加入锚点集。
        *   **环境选择**：通过非支配排序和拥挤距离，维护种群和锚点集的规模和多样性。
    3.  **终止与返回**：达到总预算上限后，返回最终的近似帕累托前沿（锚点集）。

### 3. 实验设计
*   **数据集/场景**：实验覆盖了四种不同类型的任务：
    *   `AG News`（4类话题分类）
    *   `GSM8K`（小学数学推理）
    *   `Subj`（二分类主观性判断）
    *   `MBPP`（Python代码生成）
*   **评估模型**：在三种不同规模和来源的开源LLM上进行评估：
    *   Mistral-3.2-24B
    *   Qwen3-30B
    *   GPT-OSS-120B
*   **基准对比方法**：
    *   **多目标方法**：基于NSGA-II的提示优化基线（`NSGA-II-PO`）和未经优化的初始指令集。
    *   **单目标SOTA方法**：`CAPO`（本文的主要构建基础）、`EvoPromptGA`和`GEPA`，仅用于比较最大准确率。
*   **评估指标**：
    *   **多目标指标**：噪声R2（`nR2`）、乐观与悲观超体积（`HV`）、近似差距（`Gap`）、经验达成面（EAS）图，全面衡量解集的收敛性、多样性、鲁棒性和泛化能力。
    *   **单目标指标**：测试准确率。

### 4. 资源与算力
*   **硬件配置**：所有实验均在配备至少40GB显存的**单个GPU**（NVIDIA A100 或 H100）上运行。
*   **部署工具**：使用vLLM库在本地部署和托管LLM。
*   **预算约束**：每个实验配置的优化过程均被限制在**总令牌消耗为750万（7.5×10⁶）** 的预算内，这是一个与金钱成本直接相关的客观指标。
*   **论文并未报告**完成实验所需的总GPU小时数或端到端时间。

### 5. 实验数量与充分性
*   **实验数量**：进行了大规模的实验评估，覆盖了**4个数据集 × 3个LLM模型**，共**12个主要实验配置**。每种配置使用**3个不同的随机种子**运行，以评估结果的统计稳定性和变异性。
*   **充分性与公平性评估**：
    *   **对比全面**：与多目标基线（NSGA-II-PO）、单目标SOTA方法（CAPO, GEPA等）以及未优化基线进行了多维度比较。
    *   **消融研究**：通过隔离实验，验证了“增强机制”对算法效率的关键作用，以及“成本目标”中各令牌权重的必要性。还进行了父代选择机制的消融。
    *   **超参数敏感性分析**：测试了数据块大小、种群大小和交叉率对性能的影响，表明方法对超参数的敏感性较低。
    *   **客观性**：所有优化器在同一基础框架下实现，使用相同的初始化指令、数据划分、API价格权重和严格的令牌预算，确保了对比的公平性。实验评估协议（如使用nR2、近似差距）非常严谨，考虑了泛化误差。

### 6. 论文的主要结论与发现
*   **优越的多目标性能**：MO-CAPO在12个案例中有**8个在nR2指标上超越**了NSGA-II基线，并能发现更优的超体积帕累托前沿近似，尤其在复杂任务上的提升显著。
*   **更高的优化效率**：得益于增强机制，MO-CAPO能以**远低于NSGA-II的令牌预算**（节省高达88%的初步迭代预算，快至三分之一的时间达到80%的性能）找到有竞争力的解集，同时能探索数量多达5.7倍的候选提示。
*   **多样且鲁棒的权衡方案**：MO-CAPO能发现**多样的帕累托前沿**，同时包含低成本的快速方案和高性能的昂贵方案，这些方案是单目标优化器无法提供的。并且，这些解集在未见过的测试数据上表现出良好的泛化性和鲁棒性。
*   **性能竞争力强**：MO-CAPO发现的**最高准确率提示与SOTA的单目标优化方法（如CAPO, GEPA）具有竞争力**，在统计上没有显著差异，证明引入成本目标并不会损害最佳性能。

### 7. 优点
*   **方法创新性**：将多目标自动化算法配置领域的预算分配技术（源于MO-SMAC）巧妙地引入到进化式提示优化中，解决了优化过程本身成本高昂的问题，这是对该领域的直接贡献。
*   **问题建模的现实性**：提出的成本目标同时考虑了输入和输出令牌的权重，比仅用提示长度作为成本代理的方法更贴近实际的API计费和计算开销。
*   **评估协议的先进性**：首次在多目标提示优化中采用噪声R2和近似差距等指标，系统性地评估解的泛化鲁棒性，为未来的研究设定了更严格的评估标准。实验的可视化（如经验达成面图）直观地展示了方法的稳定性和优势。

### 8. 不足与局限
*   **目标空间的局限性**：当前算法仅处理“性能”和“成本”**双目标优化**。虽然能自然扩展到更多目标，但论文未展示其在更高维度目标空间（如同时优化延迟、安全性、公平性）上的有效性。
*   **对真实成本的简化**：成本权重依赖于定价快照，未考虑实践中的复杂性，如长文本输出或高并发调用导致的动态定价与速率限制。输出令牌数间接受指令设计影响，但算法无法精确预控。
*   **可迁移性未验证**：提出的成本目标的有效性是基于作者选定的LLM和API定价权重。将该方法应用于与实验中模型性质差异很大的LLM或新任务时，权重调整和算法表现的稳健性尚待证实。
*   **与前沿技术的差距**：算法设计基于相对传统的进化算法（CAPO, NSGA-II）和配置工具（MO-SMAC），尚未整合如**代理模型（贝叶斯优化）、梯度引导（TextGrad）或新颖的反思式进化**等更前沿的提示优化思想，这可能是进一步提高效率的潜力点。

（完）
