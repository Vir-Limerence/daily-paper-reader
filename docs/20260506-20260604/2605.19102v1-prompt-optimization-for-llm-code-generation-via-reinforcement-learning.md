---
title: Prompt Optimization for LLM Code Generation via Reinforcement Learning
title_zh: 基于强化学习的大语言模型代码生成提示优化
authors: "Ali Mohammadi Esfahani, Nafiseh Kahani, Samuel A. Ajila"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19102v1"
tags: ["query:prompt-opt"]
score: 10.0
evidence: 使用PPO智能体通过混合动作空间迭代优化提示
tldr: "大语言模型代码生成对提示高度敏感，导致性能波动。本研究提出基于强化学习的提示优化框架，将提示优化建模为序列决策问题，PPO代理使用混合动作空间迭代优化提示，并由测试驱动奖励引导。在多个基准上，优化后显著提升功能正确性，DeepSeek-Coder在MBPP+上严格Pass@1达85.50%，超越EPiC等现有方法。该工作展示了强化学习自动化提示工程的潜力，为提升代码生成质量提供新途径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19102v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 4684, \"height\": 1433}]"
motivation: 大语言模型代码生成对提示高度敏感，自动优化提示是提升功能正确性的关键挑战。
method: 提出基于强化学习的提示优化框架，PPO代理通过混合动作空间和测试驱动奖励迭代改进提示。
result: "在MBPP+、HumanEval+和APPS上，三个骨干模型均显著提升，DeepSeek-Coder Pass@1达85.50%。"
conclusion: 强化学习配以测试驱动奖励可有效自动优化提示，显著提升LLM代码生成的功能正确性。
---

## 摘要
大型语言模型（LLMs）能从自然语言生成代码，但其性能对提示形式高度敏感。我们提出一个基于强化学习的框架，将提示精炼建模为序列决策问题。一个近端策略优化（PPO）智能体通过混合动作空间迭代改进提示，该空间结合了直接生成、遗传词汇变异和语义重写，并以单元测试反馈形成的形状奖励为指导。我们在MBPP+、HumanEval+和APPS上评估该框架，使用冻结的CodeT5+、CodeLLaMA和DeepSeek-Coder作为代码生成器。在包含500个任务的MBPP+测试集上，PPO智能体分别取得了57.58%、64.80%和85.50%的严格Pass@1得分，优于EPiC、Reflexion和Random-Hybrid方法。Soft-Pass@1分别达到67.90%、73.10%和88.20%。在HumanEval+和APPS上，所有骨干模型也观察到类似的改进。结果表明，具有形状化测试驱动奖励的强化学习能够提升基于LLM代码生成的功能正确性。

## Abstract
Large Language Models (LLMs) can generate code from natural language, but their performance is highly sensitive to prompt formulation. We propose a reinforcement-learning-based framework that models prompt refinement as a sequential decision-making problem. A Proximal Policy Optimization (PPO) agent iteratively improves prompts using a hybrid action space that combines direct generation, genetic lexical mutation and semantic rewriting, guided by shaped rewards derived from unit-test feedback. We evaluate the framework on MBPP+, HumanEval+, and APPS using CodeT5+, CodeLLaMA, and DeepSeek-Coder as frozen code generators. On the 500-task MBPP+ test set, the PPO agent achieves strict Pass@1 scores of 57.58%, 64.80%, and 85.50%, respectively, outperforming EPiC, Reflexion, and Random-Hybrid. Soft-Pass@1 reaches 67.90%, 73.10%, and 88.20%, respectively. Similar improvements are observed on HumanEval+ and APPS across all backbone models. The results demonstrate that reinforcement learning with shaped test-driven rewards improves functional correctness in LLM-based code generation.

---

## 论文详细总结（自动生成）

好的，以下是基于所提供的论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究背景**：大语言模型在根据自然语言描述生成代码方面表现出色，但其性能对于**提示词（Prompt）的表述方式极为敏感**。哪怕是微小的措辞变化，都可能导致生成代码的功能正确性出现巨大差异。
*   **核心问题**：现有的提示工程方法存在局限性。**静态提示**（如思维链）缺乏适应性，而**自动优化方法**（如进化算法或强化学习）通常只孤立地处理词汇或语义修改，未能将多种提示转换策略有效结合。此外，多数方法的奖励信号过于稀疏（仅依赖二元对错），忽略了部分正确的中间反馈信息。
*   **整体含义**：本研究旨在将提示优化建模为一个**序列决策问题**，利用强化学习（RL）智能体自适应地协同多种提示修改策略，并通过**蕴含丰富信息的形状奖励**来引导学习，最终提升代码生成的功能正确性。

### 2. 论文提出的方法论

*   **核心思想**：将**提示词的迭代优化**过程视为一个马尔可夫决策过程，并利用**PPO（近端策略优化）** 算法训练一个智能体。该智能体学习如何根据当前提示和反馈，动态选择最佳的修改动作，以逐步提升生成代码的质量。
*   **关键技术细节**：
    *   **状态表示**：使用 `MiniLM` 句子转换器将当前提示编码为 **384维的向量**，作为策略网络的输入。状态仅包含提示词信息，不包括生成的代码或错误追踪。
    *   **混合动作空间**：定义了三种提示转换策略，构建了一个离散的动作空间。
        1.  **直接生成**：不改变提示，利用模型自身的随机性生成代码，作为保留最佳提示的选项。
        2.  **遗传词汇变异**：受 `EPiC` 方法启发，对提示文本进行分词，通过交叉、洗牌和变异（概率0.2）等遗传算法操作，在词汇层面探索变异体。
        3.  **语义重写**：受 `Reflexion` 方法启发，利用一个固定的元提示和预训练模型对原提示进行更高层次的语义改写、澄清和重构，并通过正则表达式保留关键任务词。
    *   **形状奖励函数**：智能体用优化后的提示词生成代码，并通过单元测试进行评估。奖励不是简单的二元信号（成功/失败），而是基于通过的测试比例 \( \rho_{i,t} \) 进行精细设计：
        *   **全部通过** (\( \rho_{i,t} = 1.0 \)): 奖励 **+1.0**
        *   **部分通过** (\( 0 < \rho_{i,t} < 1.0 \)): 奖励 **\( \rho_{i,t} \)**
        *   **全部失败** (\( \rho_{i,t} = 0 \)): 奖励 **-1**
        *   **执行失败** (语法错误等): 奖励 **-2**
        *   *目的*：这种设计为部分正确的代码提供了连续、密集的学习信号，鼓励智能体逐步改进，同时严厉惩罚无效代码。
*   **算法流程**:
    1.  开始一个任务（Episode），获取初始提示。
    2.  **循环**（直到成功或达到最大步数）：
        *   智能体根据当前提示的状态 \( s_{i,t} \) 采样一个动作 \( a_{i,t} \)。
        *   应用对应的转换函数 `T` 得到新提示 \( p_{i, t+1} \)。
        *   用冻结的（frozen）代码生成大模型和新提示生成代码。
        *   在沙盒中执行代码，根据单元测试结果计算**形状奖励**。
        *   存储状态转移数据，用于后续的PPO策略更新。
    3.  更新PPO智能体的策略网络参数 \( \theta \)。

### 3. 实验设计

*   **数据集与场景**：
    *   **MBPP+**：包含974个任务，按374/500划分训练/测试集，用于评估基础编程能力。
    *   **HumanEval+**：用于评估中等难度功能正确性的标准基准测试，按80%/20%划分。
    *   **APPS**：包含更高算法复杂度的竞赛级编程问题，按80%/20%划分。
*   **评估指标**：
    *   **严格 Pass@1**：主要评估指标，要求生成的代码通过**所有**单元测试才算成功。
    *   **Soft Pass@1**：辅助诊断指标，衡量在多步优化中趋向完全正确的累积进展，不视为最终成功。
*   **对比方法**：
    *   **直接生成类**：单步的`Direct Generation`、`Genetic Mutation`、`Semantic Rewriting`。
    *   **迭代优化类**：`EPiC`（基于进化搜索）、`Reflexion`（基于语言自我反思）、`Random-Hybrid`（使用本研究同样的混合动作空间但随机选择动作，作为消融实验基线）。
    *   **消融研究**：对比了**稀疏二元奖励**和**形状奖励**对PPO训练效果的影响。
*   **骨干模型（代码生成器）**：`CodeT5+`（2.2亿参数）、`CodeLLaMA`（70亿参数）、`DeepSeek-Coder`（67亿参数）。所有模型在训练和评估期间均保持**冻结状态**。

### 4. 资源与算力

*   **文中未明确说明**：在提供的文本中，没有提及用于训练PPO智能体或评估代码生成模型所使用的GPU型号、数量或具体的训练时长。这构成了论文信息的一个缺失点。

### 5. 实验数量与充分性

*   **实验组合**：实验设计周全，涵盖了**3个数据集 × 3个骨干模型 × 多种方法**的交叉评估，构成了一个全面的测试矩阵。
*   **实验层次**：
    1.  **单步性能评估**：测试了3种单一动作的效果。
    2.  **奖励函数消融**：对比了形状奖励与稀疏二元奖励，验证了奖励设计的关键作用。
    3.  **多步策略对比**：将PPO与`EPiC`、`Reflexion`及关键的`Random-Hybrid`进行比较，后者有效排除了“仅靠混合动作空间就能带来增益”的干扰，证明了**学习调度策略**的必要性。
*   **充分性与公平性**：实验设计较为充分和客观。通过使用公开基准、固定骨干模型、引入`Random-Hybrid`对比基线以及统计显著性检验（McNemar's test， p < 0.001），保证了对比的公平性和结论的可靠性。

### 6. 论文的主要结论与发现

*   **PPO框架表现最优**：在所有数据集和所有骨干模型上，PPO方法的严格`Pass@1`和`SoftPass@1`得分均**显著优于**`EPiC`、`Reflexion`和`Random-Hybrid`。例如，在MBPP+上，使用DeepSeek-Coder时，PPO的严格`Pass@1`达到了**85.50%**，比最佳基线有显著提升。
*   **学习调度至关重要**：PPO相比`Random-Hybrid`的巨大优势表明，性能提升并非仅仅因为混合动作空间本身，而在于**策略学会了何时应用何种转换**（词汇变异或语义重写）的自适应调度能力。
*   **形状奖励优于稀疏奖励**：消融研究证实，基于部分正确性的**形状奖励**相比仅依赖最终成功的稀疏二元奖励，能提供更稳定的学习信号，显著提升训练效率和最终性能。
*   **语义重写是有效的单步策略**：在没有迭代的单步测试中，`Semantic Rewriting（语义重写）`是所有单一策略中最有效的，表明减少歧义的提示能直接提高代码生成质量。

### 7. 优点

*   **新颖统一的框架**：首次将遗传词汇变异和语义重写**统一到强化学习的混合动作空间中**，由可学习的策略进行序贯决策。
*   **精细的奖励设计**：引入**形状奖励**，有效利用了单元测试中的部分正确信息，为稀疏奖励环境下的策略学习提供了更密集的指导信号，是方法成功的关键之一。
*   **严格的实验验证**：实验设计非常扎实。`Random-Hybrid`基线的引入是一个亮点，巧妙地分离了“动作空间”和“动作调度”各自的贡献，有力地证明了核心方法的优越性。同时，在多个模型和数据集上的评估也证明了其鲁棒性。

### 8. 不足与局限

*   **状态表示有限**：PPO智能体的状态仅基于**提示词本身的嵌入**，不包含任何来自代码执行失败的信息，如断言错误、运行时异常或错误跟踪。这限制了其进行更深层次、诊断性调试的能力。
*   **计算成本较高**：方法论部分提到，语义重写和PPO训练涉及“重复的生成、执行和奖励计算”，相较于静态提示或一次性优化，**引入了额外的计算开销**。
*   **算力信息缺失**：论文未报告训练所需的算力资源（如GPU型号、时长），这使得**方法的可复现性和计算成本难以评估**。
*   **应用范围限制**：框架仅在代码生成任务上进行了验证。其结论能否推广到其他大语言模型应用场景（如数学推理、通用问答），还有待研究。
*   **外部依赖性**：性能在一定程度上依赖于用于语义重写的独立语言模型的质量和元提示的设计。

（完）
