---
title: "Reinforced Graph of Thoughts: RL-Driven Adaptive Prompting for LLMs"
title_zh: 强化图思维：面向大语言模型的强化学习驱动自适应提示
authors: "Manuel Noah Riesen, Peter Alfred von Niederhäusern"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22195v1"
tags: ["query:prompt-opt"]
score: 9.0
evidence: 强化学习驱动的自适应提示自动生成思维图以解决问题
tldr: 现有Graph of Thoughts依赖人工定义静态操作图，缺乏对任务复杂度的适应性。本文提出Reinforced Graph of Thoughts，利用强化学习从预定义操作集合中动态生成操作图。实验表明，在约束条件下，该方法能根据任务需要自动调整图的构建。这实现了自适应提示，提升了LLM解决复杂问题的自动化水平。
source: arxiv
selection_source: fresh_fetch
motivation: Graph of Thoughts需手动设计操作图，限制了其对不同任务复杂度的灵活性。
method: 从人类定义的操作集合中，使用强化学习自适应生成操作图。
result: 在特定约束下，可自动构建与任务复杂度匹配的操作图。
conclusion: 该方法推动了提示范式的自动化，使LLM推理更灵活高效。
---

## 摘要
图思维（GoT）作为大语言模型（LLM）近期提示范式的一种泛化形式，已被证明有助于精细问题的求解。通过执行操作图，大语言模型的思维被组织为任意图，形成实际的思维图。最初，操作图是人工定义的，这需要深入了解待解决问题的解决方案。这种静态操作图较为僵化，因而缺乏适应性。我们提出强化图思维（RGoT），一种GoT提示范式的自动化方法，利用强化学习（RL）从人工定义的集合中自适应地生成操作图。结果表明，在一定约束条件下，能够以自动化方式根据任务复杂度自适应地构建操作图。

## Abstract
Graph of Thoughts (GoT), a generalized form of recent prompting paradigms for large language models (LLMs), has been shown to be useful for elaborate problem solving. By executing a graph of operations, thoughts of the LLM are structured as an arbitrary graph, forming the actual graph of thoughts. Originally, the graph of operations is defined manually, which requires in-depth knowledge about the solution of the problem to solve. Such a static graph of operations is rigid and therefore lacks adaptability. We propose Reinforced Graph of Thoughts (RGoT), an automated approach to the GoT prompting paradigm that leverages reinforcement learning (RL) to adaptively generate a graph of operations from a human-defined set. Results indicate that, under certain constraints, it is possible to construct graphs of operations adaptively to the task's complexity in an automated way.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：现有图思维（Graph of Thoughts, GoT）提示范式需要人工预先定义完整的操作图，既要求使用者对问题解法有深度认知，又导致推理流程的静态与僵化，无法根据任务复杂度或语言模型的随机输出进行自适应调整。
- **整体含义**：论文旨在实现图思维范式的自动化与自适应，提出强化图思维（Reinforced Graph of Thoughts, RGoT），利用强化学习从人工定义的操作集合中动态构造操作图，使大语言模型（LLM）的推理拓扑能够根据任务实例的复杂度自动演化，提升复杂问题求解的灵活性与可及性。

### 2. 论文提出的方法论
- **核心思想**：将操作图的逐步构建建模为马尔可夫决策过程（MDP），强化学习智能体在当前图状态的基础上选择下一步操作（追加、回溯或停止），最终生成有向无环的操作图，以执行思维生成。
- **关键技术与流程**：
  - **纯 GoT 框架**：重新设计独立的图思维框架，任务由一组操作（含提示操作与程序化执行操作）及评分操作组成。操作图被约束为层状 DAG：单一源、单一汇、层间稠密连接，深度与广度存在上限，并在截断深度后强制收敛。
  - **任务复杂度定义**：每个任务实例有标量复杂度（如列表长度、文档数），复杂度直接关联各个操作的 LLM 解决成功率。
  - **LLM 仿真模型**：为解决 RL 训练的样本效率问题，用伯努利试验模拟特定复杂度下操作成功的概率 \( \hat{P}(c) \)，概率通过重复调用真实 LLM（gpt-3.5-turbo-0125）估计得到。训练与评估均在仿真环境中进行。
  - **RL 环境设计**：
    - **动作空间**：离散动作集 \( \{\text{stop}, \text{backtrack}\} \cup \{\text{append}(o) \mid o \in \mathcal{O}\} \)，按层追加操作。
    - **观测空间**：包含图深度、当前广度、任务总复杂度、局部复杂度、图表示的 one-hot 向量、历史动作、上次评分结果。
    - **奖励函数**：经过多版本塑造，融合深度惩罚、无效行为惩罚、回溯奖励、评分成功奖励等，最终版本在部分情境下给予正反馈。
  - **RL 算法**：最终选用 PPO（近端策略优化），使用简单的全连接网络（actor 和 critic 均为 [64, 64]），配合线性衰减学习率、clip range 调温器与熵正则化。
  - **图构造启发式**：保证 DAG 深度与广度上限，并在计算出的截断深度后禁止分叉，确保图最终收敛到单一汇点。

### 3. 实验设计
- **任务与数据集**：
  - `sum list`：计算单个数字列表之和，复杂度为列表长度。
  - `sort list`：对数字列表排序，复杂度为列表长度。
  - `count keywords`：统计文本中国家关键词出现次数，复杂度为单词数；使用 Besta 等人的数据集与自定义采样逻辑。
  - `intersect set`：计算两个集合的交集，复杂度为集合大小。
  - `merge documents`：合并多篇文档，最大化内容保留并最小化冗余，复杂度为文档数；使用 Besta 等人的数据集并以 ROUGE-L 和 ROUGE-1 进行评分。
- **对比基准与方法**：
  - 主要对比：Input‑Output（IO）提示，即直接让 LLM 解决任务而不进行任何分解。
  - 评估指标：各复杂度下的**解决率**（solved rate），评估包含训练分布内和高于训练复杂度的分布外（OOD）数据。
- **训练与测试设置**：
  - 训练复杂度范围有限（图中以垂直虚线标出），测试则延伸至更高复杂度，以检验智能体的泛化能力。
  - 每个任务训练多个不同种子（5个种子）以评估方差。
  - RL 算法对比了 DQN、A2C 与 PPO，最终选用 PPO 作为主算法。

### 4. 资源与算力
- 论文**未明确提及**所使用的 GPU 型号、数量及训练时长。
- 提及的技术参数包括：
  - 训练总步数：\(2^{18} = 262,144\) 步。
  - 并行环境数：8 个向量化环境。
  - 评估真实 LLM 时，每个复杂度级别使用 100 次查询来估计成功率 \( \hat{P}(c) \)。
- 整体计算负担相对较低：RL 训练在仿真器上运行，成本远小于直接与 LLM 交互训练。

### 5. 实验数量与充分性
- **实验组数**：
  - 5 个不同任务，每个任务训练 5 个随机种子，共 25 组独立训练。
  - 每个任务的 LLM 仿真概率曲线源自各操作在多个复杂度上的 100 次调用。
  - 附录中提供了消融实验（I 节），对 PPO 的关键超参数（clip range 调度、更新轮数、学习率衰减、网络结构等）进行了对比，论证了最终配置的合理性。
- **充分性与公平性**：
  - 多任务、多种子设计降降低了偶然性，结果报告了解平均值和标准差。
  - 在对比中，仅与 IO 基线比较，未与人工设计的静态 GoT 对比，也未与其它自动化 GoT 方法（如 Auto‑GoT）直接比较；论文声明仅在概念上区分，不作数值比较。
  - 实验完全在仿真环境中进行，未在真实 LLM 交互环境下验证训练得到的智能体性能，是一个显著的局限。
  - 消融实验较详细，但未报告不同随机种子下策略的失效比例或极端值分析。

### 6. 论文的主要结论与发现
- RGoT 能够在给定约束下，以自动化方式根据任务复杂度自适应构建操作图。
- 智能体训练后在分布内任务上平均解决率达 0.9399，在分布外复杂度上仍能泛化，平均解决率 0.7512，显著优于 IO 基线。
- 复杂度的增加促使智能体学习使用更多操作（如分支与合并），而非单纯调用主操作；这一行为在训练未见过的复杂度上依然出现，表明了一定的泛化能力。
- 方法对底层语言模型完全无依赖，可通过仿真任意 LLM 的操作成功分布来训练智能体，具有模型无关性。
- 方差分析显示，PPO 的在线性质及稀疏奖励导致智能体在不同种子下性能波动较大，尤其在分布外区域表现不稳定。

### 7. 优点
- **自动化与自适应**：将原本完全依赖人工设计的 GoT 构建过程交给 RL 智能体，降低了使用门槛，并使推理拓扑能根据输入复杂度动态变化。
- **模型无关性**：框架与具体 LLM 解耦，只需提供各操作的成功概率曲线即可训练，可灵活迁移到不同模型甚至未来更强模型。
- **仿真训练高效**：通过在仿真环境中训练，极大节省了直接调用 LLM 的成本和时间，使 RL 的训练变得可行。
- **表达与约束**：重新设计的纯 GoT 框架将操作图约束为层状 DAG，既保留了图结构的表达能力，又简化了图构建的决策空间，便于 RL 学习。
- **多任务验证**：在排序、计数、集合交、文档合并等多种任务上进行了验证，显示了方法的通用性。

### 8. 不足与局限
- **仅仿真评估**：训练及评估完全基于操作成功概率的仿真器，未在真实 LLM 交互环境下验证智能体生成的图能否真正提升端到端任务性能。仿真偏差可能高估或低估实际效果。
- **操作图拓扑受限**：强制层状 DAG、单一源汇、广度深度上限及截断因子等约束，限制了图拓扑的多样性，可能不适合需要复杂非层状推理结构的任务。
- **任务与操作集合简单**：实验任务的操作数量较少（通常 2‑4 个），难度有限；未涉及需要更复杂操作的开放域任务。
- **泛化能力波动大**：PPO 在线样本特性及稀疏奖励导致不同种子间性能差异显著，尤其在分布外复杂度上，方法的鲁棒性有待增强。
- **缺少直接对比**：未与人工精心设计的 GoT 静态图进行对比，也未与已有自动化方法（如 Auto‑GoT）进行定量比较，因此难以判断 RGoT 相对于已有自动化方案的优势程度。
- **计算资源报告缺失**：未说明训练所用的硬件配置及耗时，可复现性受影响。
- **超参数敏感**：消融实验表明训练对学习率、clip range 以及总步数敏感，可能需要针对每个任务细致调参才能获得稳定表现。

（完）
