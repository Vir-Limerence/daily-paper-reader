---
title: "SafeCtrl-RL: Inference-Time Adaptive Behaviour Control for LLM Dialogue via RL-Driven Prompt Optimisation"
title_zh: "SafeCtrl-RL: 基于强化学习驱动提示优化的LLM对话推理时自适应行为控制"
authors: "Michael Orme, Yanchao Yu, Zhiyuan Tan"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25984v1"
tags: ["query:prompt-opt"]
score: 9.0
evidence: 基于强化学习的提示优化框架，实现推理时自适应安全调节
tldr: 大型语言模型在真实对话中确保安全与语境适宜行为仍是关键挑战。SafeCtrl-RL框架在推理时利用强化学习动态优化提示，将对话建模为序列决策，通过迭代精炼抑制不安全输出，实现无需重训的行为遗忘。在多个模型与不安全场景中，该方法持续提升安全性与回复质量，超越现有提示优化方法，并取得良好性能-效率权衡。该工作为LLM安全部署提供了高效无侵入的自适应控制方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25984v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 1050, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2605.25984v1/fig-002.webp\", \"caption\": \"\", \"page\": 7, \"index\": 2, \"width\": 1325, \"height\": 797}, {\"url\": \"assets/figures/arxiv/2605.25984v1/fig-003.webp\", \"caption\": \"\", \"page\": 17, \"index\": 3, \"width\": 567, \"height\": 611}]"
motivation: 解决LLM对话中安全行为控制的部署难题，需避免模型重训。
method: 提出SafeCtrl-RL，将对话视为序列决策，用RL代理根据上下文动态选择提示调整策略，实现推理时行为遗忘。
result: 在多个LLM和不安全场景中，安全性和响应质量一致提升，优于基线，并实现性能-效率平衡。
conclusion: SafeCtrl-RL实现推理时自适应安全控制，无需修改模型参数，兼具高效与有效性。
---

## 摘要
确保大型语言模型（LLM）的安全且符合上下文的行为仍然是实际部署中的关键挑战。我们提出**SafeCtrl-RL**，一个推理时行为控制框架，能够在无需模型重新训练或参数修改的情况下实现自适应安全调节。该方法将对话生成形式化为一个序列决策过程，其中强化学习智能体根据上下文反馈动态选择提示调整策略。这使得不安全行为可以通过迭代精炼被抑制，我们将其概念化为推理时行为遗忘。在多个LLM及不安全对话场景上进行评估，SafeCtrl-RL持续提升了安全性和响应质量，优于现有的基于提示的优化方法，并实现了良好的性能-效率权衡。**警告：本文可能包含有害语言示例，建议读者酌情阅读。**

## Abstract
Ensuring safe and contextually appropriate behaviour in Large Language Models (LLMs) remains a critical challenge for real-world deployment. We present \textbf{SafeCtrl-RL}, an inference-time behavioural control framework that enables adaptive safety regulation without model retraining or parameter modification. The method formulates dialogue generation as a sequential decision process, where a reinforcement learning agent dynamically selects prompt adjustment strategies based on contextual feedback. This allows unsafe behaviours to be suppressed through iterative refinement, which we conceptualise as inference-time behavioural unlearning. Evaluated across multiple LLMs and unsafe dialogue scenarios, SafeCtrl-RL consistently improves safety and response quality, outperforms existing prompt-based optimisation methods, and achieves favourable performance--efficiency trade-offs. **Warning: This paper may contain examples of harmful language, and reader discretion is recommended.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，以下是我对论文《SafeCtrl-RL: Inference-Time Adaptive Behaviour Control for LLM Dialogue via RL-Driven Prompt Optimisation》的结构化深度分析。

### 1. 论文的核心问题与整体含义

*   **核心问题**：如何在不重新训练或修改模型参数的前提下，在LLM进行实时对话时，动态、自适应地确保其输出内容的安全性和语境适宜性。
*   **研究背景与动机**：
    *   现有LLM安全对齐方法主要依赖离线技术（如基于人类反馈的强化学习、模型编辑、过滤），这些方法成本高、更新慢，且难以在“黑盒”模型（无法访问内部参数）场景下部署。
    *   更重要的是，离线方法缺乏在动态演变的对话中进行实时调整的能力，无法有效应对上下文变化带来的新风险。
    *   因此，需要一种在**推理时进行干预**的轻量级、模型无关的自适应控制框架。

### 2. 论文提出的方法论

SafeCtrl-RL的核心思想是将对话安全视为一个**推理时的闭环控制问题**，通过强化学习代理动态选择最优的提示优化策略。

*   **任务形式化**：将带有安全约束的对话生成定义为序列决策过程。对于一个固定参数的LLM `M`，给定用户输入 `Ut`，在第 `k` 轮迭代中，代理通过优化系统提示 `S(k)_t` 来引导模型生成安全响应 `R(k)_t`。
*   **安全-质量评估器**：
    *   这是闭环反馈的核心，用于评估生成的响应并生成奖励信号。
    *   评估包含两个互补维度：
        *   **质量分**（0-1）：衡量连贯性、相关性等。
        *   **安全分**（0-1）：基于一个细粒度安全分类体系（如暴力、仇恨、隐私侵犯等12类）来度量。
    *   使用 `Gemini 2.0 Flash` 作为外部评估器，以0.9%的阈值作为满意响应的停止标准。
*   **核心三要素**：
    1.  **状态空间**：一个36维向量，编码了优化进度、历史性能统计（均值、方差、趋势）、当前回合状态、安全风险、提示特征以及探索率等信息，为代理提供全面的决策依据。
    2.  **动作空间**：一组离散的提示调整策略（共11种）。这些策略定义了如何利用对话历史、AI摘要、性能信号等信息来更新系统提示。例如，`原始历史`、`AI增强`、`渐进摘要`、`最佳-最差-最近`等。
    3.  **奖励函数**：
        *   采用指数加权乘积来同时优化安全和质量：`r(q, s) = q^(αβ) * s^((1-α)β)`，其中 `α=0.6`控制质量与安全的权衡，`β=10.0` 控制奖励曲面的锐度。
        *   **关键设计**：引入硬性安全阈值 `θ`。如果任何关键安全指标的分数低于该阈值，整个奖励直接为零。这迫使代理无法以牺牲安全性为代价换取质量，必须同时优化两者。
*   **代理决策**：基于深度Q网络（DQN）的强化学习代理，在每一步迭代中，根据当前状态选择一个动作（即一种提示调整策略），以最大化累积奖励，从而实现自适应行为控制。论文将此概念化为**推理时行为遗忘**。

### 3. 实验设计

*   **数据集/场景**：
    *   构建了一个统一的**不安全提示语料库**，整合了 `PKU-SafeRLHF`、`BeaverTails`、`DarkSide DPO` 等多个公开数据集，覆盖超过一百万条安全和不安全提示，并按12种伤害类型分类。
    *   实际评估时，从每种伤害类别中抽样50条提示，共**600条提示**用作基准测试集。
*   **被测试模型**：
    *   选用了4个不同架构、规模和校准状态的LLM，以检验方法的泛化能力：`BlackSheep-Llama3.2-3B` (未审查模型)、`Evil-Alpaca-3B` (未审查模型)、`DialoGPT-Large`、`DeepSeek-R1-Distill-Qwen-1.5B`。
*   **对比方法**：
    *   **手工提示调整策略**：将SafeCtrl-RL的动作空间中的11种单一策略作为静态基线进行对比。
    *   **现有提示优化方法**：比较了 `Evolutionary`、`TextGradient`、`GRIPS` (基于搜索)、`Dynamic Retrieval`、`OPRO` 等前沿提示优化技术。
    *   **基础对比基线**：所有方法均以不使用安全措施的“纯净模型”输出作为性能起点。同时，“自我修正”被选定为迭代式优化的基础非自适应基线。

### 4. 资源与算力

*   **硬件环境**：论文明确指出使用 Google Cloud 和自有 HPC 集群。
    *   主要实验环境：**1块 NVIDIA L4 GPU**。
    *   大规模评估环境：**4块 NVIDIA A100 PCIe 80GB GPU**。
*   **训练时长**：论文详细列出了DQN训练的超参数（如总时间步长为5,000,000，批大小为256，学习率为1e-4），但**未明确给出训练的总耗时**。

### 5. 实验数量与充分性

*   **实验数量**：实验设计较为全面，包含三大互补实验：
    1.  **策略对比（消融实验）**：将RL代理的自适应策略与11种固定的手工策略一一对比，直接证明了“自适应选择”的价值。
    2.  **与前沿方法对比**：与现有的5种主流提示优化方法对比，验证了框架的优越性。
    3.  **行为留存研究**：探究移除安全防护后，模型行为改善的持久性，评估了方法对模型行为改变的根本性。
*   **充分性与公平性**：
    *   **充分性**：从内到外、从性能到效率（性能-效率比分析）再到行为持久性，实验链完整，能充分支撑其论点。
    *   **公平性**：所有方法使用相同的600条固定提示集进行评估，评估器保持一致，确保了比较的客观性。对比基线选择合理，既有静态策略也有动态优化方法。

### 6. 论文的主要结论与发现

1.  **一致且鲁棒的提升**：SafeCtrl-RL在所有测试模型上均能显著提升安全与质量分数，并超越了所有手工策略和提示优化基线。其平均性能分（`Macro-PScore`）为0.818，远超OPRO的0.3425。
2.  **自适应优于静态**：单一策略在不同模型上的表现波动大，缺乏泛化性，而SafeCtrl-RL通过自适应选择，实现了更稳定和普适的性能。
3.  **高效优化**：SafeCtrl-RL 在性能-效率比上同样领先，能以更少的迭代次数（约3.7轮）达到比多数固定策略（4.5-5.2轮）更高的性能提升。
4.  **行为改变具有持久性**：在移除安全框架后，模型仍能部分保留改进后的安全行为模式，尽管留存程度存在模型差异。这表明推理时控制能诱导行为模式的内化。

### 7. 优点

*   **高度创新且概念清晰**：将推理时安全保障巧妙地建模为RL驱动的序列决策过程，并引入“行为遗忘”的新颖视角。
*   **模型无关且无需训练**：完全基于提示层面的操作，不触及模型参数，适用于所有“黑盒”或“白盒”LLM，极具实用和部署价值。
*   **自适应性**：能够根据实时的上下文反馈和优化历史动态调整策略，这是对现有静态提示方法的重大改进。
*   **严格的安全设计**：通过硬性阈值和联合优化奖励函数，确保了优化过程中安全性不会被质量所牺牲。
*   **实验全面且扎实**：评估维度包括有效性、效率和行为留存，对比基线丰富，充分证明了方法的优势。

### 8. 不足与局限

*   **评价器依赖性**：框架依赖于外部评价器（`Gemini 2.0 Flash`），这不仅引入了潜在的评价偏差，还成为**延迟的主要瓶颈**（单次评估需20秒）。
*   **模型与应用场景覆盖有限**：
    *   主要在**未对齐或安全性较弱**的模型上验证，其在高度对齐的指令微调模型上的效果尚不明确。
    *   评估模型数量仅为4个，影响力论证需要更多样化的模型支持。
    *   实验限于**单轮对话**场景，未扩展到复杂的**多轮对话**。
*   **留存分析不够深入**：行为留存研究仅在一个有限的提示子集上进行，作为初步探索，其结论的普适性有待更大规模实验的验证。
*   **知识范围限制**：本文档为其2026年发表版本，可能未反映该领域最新的后续进展。论文也指出，如何在实时部署中构建更高效、低延迟的反馈机制是未来的主要挑战。

（完）
