---
title: "SPEAR: Code-Augmented Agentic Prompt Optimization"
title_zh: SPEAR：代码增强的代理式提示优化
authors: "Mengyin Lu, Cong Feng, Huimin Han, Guangming Lu, Yu Sun, Xiaonan Ding, Shihui Long, Fengyi Li, Tanvi Motwani"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26275v1"
tags: ["query:prompt-opt"]
score: 10.0
evidence: 提出带有Python沙箱的智能体式自动提示优化系统，用于结构化错误分析。
tldr: 自动提示工程中现有优化器自身流程固定，缺乏自主分析能力。SPEAR将代码即行动范式引入提示优化，打造自由形式智能体，通过Python沙箱主动执行结构化错误分析，辅以回滚护栏实现稳定改进。在三个工业级LLM法官套件和BBH等基准上，SPEAR在所有任务上取得最优成绩，Python工具成为关键驱动。该方法揭示了代码增强在自动化提示设计中的核心价值，显著提升复杂评估的可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26275v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 1600, \"height\": 1054}, {\"url\": \"assets/figures/arxiv/2605.26275v1/fig-002.webp\", \"caption\": \"\", \"page\": 10, \"index\": 2, \"width\": 2274, \"height\": 762}]"
motivation: 现有自动提示工程优化流程固定，缺乏对评估结果的自主结构化分析，限制了优化潜力。
method: 将代码即行动范式引入提示优化，构建配备 Python 沙箱和回滚机制的智能体，自主进行结构化错误分析并迭代改进提示。
result: 在13项工业法官任务和BBH等基准上全面超越基线，工具选择κ达0.857，BBH-7准确率0.938；消融表明Python工具贡献最大。
conclusion: SPEAR 创新性地结合代码执行与提示优化，通过自主错误分析实现稳健改进，为自动化提示工程开辟新范式，尤其在复杂评估任务中展示了显著优势。
---

## 摘要
自动提示工程（APE）通过改写提示来提升下游任务性能，但现有的APE循环将优化器本身视为固定流程。我们将CodeAct（Wang等人，2024a）的“代码即行动”范式引入APE，提出SPEAR（带主动回滚的沙盒提示工程师），这是一种自由形式的代理型优化器，拥有四个工具——evaluate、python、set_prompt、finish——并自主决定何时以及如何使用它们。其独特之处在于Python沙箱：优化器在当前评估DataFrame上编写并执行任意Python代码，进行代理自身编写的结构化误差分析（混淆矩阵、错误聚类、分组指标）。两个防护措施将长程代理转变为单调改进的优化器：指标退化时自动回滚，以及可选的防护指标下限。我们在三个工业级LLM裁判套件（涵盖招聘筛选、对话记忆和查询优化系统中的13个裁判任务）以及七个BBH任务和GSM8K上进行了评估。SPEAR在所有工业任务的主要指标上均获胜（工具选择上的κ系数0.857对0.359；过滤相关性上的宏平均F1 0.815对0.763；在最难的抽取维度上κ系数0.254对0.218）。在BBH-7上，SPEAR平均准确率为0.938，而GEPA为0.628，TextGrad为0.484。消融实验表明，Python工具在复杂裁判任务上是影响力最大的单一因素（在5分类工具选择裁判上Δ≈+0.79κ，移除后在最难的抽取维度上Δ≈+0.35κ）；其不可替代的贡献在于类别对混淆聚合，这是长上下文LLM无法从原始评估DataFrame中可靠提取的。

## Abstract
Automatic prompt engineering (APE) rewrites prompts to improve downstream task performance, but existing APE loops treat the optimizer itself as a fixed pipeline. We port the code-as-action paradigm of CodeAct (Wang et al., 2024a) to APE and propose SPEAR (Sandboxed Prompt Engineer with Active Roll-back), a free-form agentic optimizer with four tools -- evaluate, python, set_prompt, finish -- that decides autonomously how and when to use them. The distinctive tool is the Python sandbox: the optimizer writes and executes arbitrary Python on the current evaluation DataFrame, performing structural error analysis (confusion matrices, error clustering, per group metrics) the agent itself authors. Two guardrails turn the long-horizon agent into a monotone-improving optimizer: auto-rollback on metric regression, and an optional guard metric floor. We evaluate on three industrial LLM-as-judge suites (13 judge tasks across recruiter-intake, conversational-memory, and query-refinement systems) plus seven BBH tasks and GSM8K. SPEAR wins every industrial task on the primary metric ($κ$ 0.857 vs 0.359 on tool-selection; F1-macro 0.815 vs 0.763 on filter-relevance; $κ$ 0.254 vs 0.218 on the hardest extraction dimension). On BBH-7 SPEAR averages 0.938 accuracy vs GEPA 0.628 and TextGrad 0.484. Ablations show the Python tool is the largest single lever on complex judge tasks ($Δ\approx +0.79κ$ on the 5-class tool-selection judge, $Δ\approx +0.35κ$ on the hardest extraction dimension when removed); its irreplaceable contribution is class-pair confusion aggregation that a long-context LLM cannot extract reliably from the raw eval DataFrame.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化中文总结。

### 1. 核心问题与研究动机
*   **核心问题**：现有的自动提示工程（APE）系统通常将优化器本身设计成一个**固定的流程管道**。其错误信号（如标量损失）和分析能力在系统设计时就已固化。
*   **研究动机**：当模型的错误呈现出结构化的模式（例如，混淆了某两个特定类别，或者存在特定规则的矛盾）时，固定的管道无法向优化器暴露这些深层的结构信息，从而限制了优化的潜力。
*   **整体含义**：论文旨在打破这种固定管道，通过引入**代码的自主执行能力**，让优化器能够动态地、深入地对评估数据进行分析，从而发现并修复更复杂的错误模式。

### 2. 方法论：SPEAR 系统
SPEAR 的核心是将 **代码即行动（Code-as-action）** 的范式引入 APE，构建一个自由形式的智能体优化器。
*   **核心思想**：优化器不再是一个被动接收反馈的固定管道，而是一个能够主动编写并执行 Python 代码来分析错误的智能体。
*   **四个工具**：
    *   `evaluate`: 对当前提示在训练集或验证集上进行评分，并返回每个样本的预测结果。
    *   `python`: （核心创新）在沙盒环境中执行智能体自己编写的任意 Python 代码，直接操作包含评估结果的 DataFrame，进行结构化分析（如生成混淆矩阵、错误聚类、分组统计）。
    *   `set_prompt`: 替换当前的系统提示。
    *   `finish`: 终止优化循环。
*   **关键技术细节与流程**：
    *   **自主决策**：智能体在每个步骤中自主选择调用哪个工具，没有固定的“评估-分析-重写”顺序。
    *   **防护机制**：
        1.  **自动回滚（Auto-rollback）**：当新提示在验证集上的主要指标低于历史最佳值时，自动恢复为最佳提示。
        2.  **防护指标下限（Guard-metric floor）**：可选项，确保重写后的提示在一个辅助指标上不低于预设阈值（如精确率），防止为提升主指标而损害其他关键性能。
*   **算法流程**：优化器从一个初始提示开始循环运行。在预算内，它可以随时调用 `python` 工具对上一次 `evaluate` 得到的 DataFrame 进行分析，然后根据分析结果调用 `set_prompt` 重写提示，并再次 `evaluate`。这个循环由指标回滚机制守护，确保性能单调不减（至少在最好的检查点上是这样）。优化目标是在给定的评估次数预算内最大化主要指标。

### 3. 实验设计
*   **数据集与场景**：
    *   **工业级 LLM 裁判套件（3个套件，13个任务）**：
        1.  **招聘助理 (Hiring Assistant)**：10个抽取评估维度（如工作地点、雇佣类型），带有经专家裁决的三位标注员标签。
        2.  **对话记忆代理 (CMA)**：2个工具选择评估任务，为5分类标签。
        3.  **Facet 建议**：1个查询优化系统的过滤相关性评估，为二分类任务。
    *   **公共基准**：
        1.  **BBH-7**: 从 BIG-Bench Hard 中挑选的7个 OPRO 风格任务（如逻辑推理）。
        2.  **GSM8K**: 小学数学应用题数据集。
*   **对比方法（Baselines）**：
    *   **TextGrad**: 基于文本梯度的提示优化系统。
    *   **GEPA**: 基于反思性进化的提示优化系统。
    *   注：未直接对比 DSPy/MIPRO，因其搜索空间包含少样本示例，而论文的 LLM 裁判场景会因此导致标签泄露。
*   **实验设置**：
    *   任务模型均为 GPT-4o (`T=0`)。
    *   SPEAR 和 GEPA 的优化器模型使用 GPT-5.4，TextGrad 因技术问题使用 GPT-4o（并补充了统一优化器模型的消融实验）。
    *   采用自动提示工程领域标准的 `best-of-N` 报告方式。所有方法的评估总调用次数保持可比。

### 4. 资源与算力
*   论文**未明确提及**所使用 GPU 的型号、数量或总训练/优化时长。
*   成本分析部分提到，SPEAR 通常在 2-3 次完整评估后收敛，而 GEPA 需要约 500 次评估调用。在三个工业数据集上，SPEAR 每个任务的总花费（美元）与 GEPA 相差在 17% 以内，这表明虽然 SPEAR 的单次分析成本可能更高，但其极快的收敛速度使得总成本具有竞争力。

### 5. 实验充分性与公平性
*   **实验组数量**：实验设计较为全面。
    *   **主实验**：对比了 3 种方法在 14 个任务上的性能（13个工业 + BBH-7），并评估了 GSM8K。
    *   **关键消融实验**：设置了 6 种消融实验来分离各组件的作用（如移除 Python 工具、移除自主循环、降级优化器模型等）。
    *   **补充分析**：包括收敛轨迹分析、跨模型迁移性、方差分析（多种子运行、留出测试集）、以及统一优化器模型的公平对比。
*   **公平性**：
    *   **优点**：提供了统一的优化器模型（GPT-5.4）下的对比，排除了优化器能力不对称的影响。评估总调用次数设计为对所有方法可比。
    *   **风险**：主要结果的 `best-of-N` 报告方式可能导致对 SPEAR 有利的偏差，因为论文中 SPEAR 在某些任务上的总运行次数（`N_total`）多于基线。作者虽然指出了这一点并提供了单种子运行和方差分析来缓解担忧，但主表中 SPEAR 在搜索池上的优势依然存在。此外，SPEAR 在验证集上能看到聚合的标签分布，这与基线方法（TextGrad等）信息不对称，可能带来轻微优势（作者也对此进行了讨论）。

### 6. 主要结论与发现
*   **全面领先**：SPEAR 在所有工业级 LLM 裁判任务的主要指标上均超越了 TextGrad 和 GEPA，在难任务上优势巨大（如地点抽取任务的κ系数：0.76 vs. 0.11/0.36）。
*   **BBH 优势源于格式修正**：SPEAR 在 BBH-7 上的大幅领先主要由其一步到位的输出格式重写驱动；在格式匹配的 GSM8K 上，所有方法表现持平，说明该架构优势在深层推理上的作用有限。
*   **Python 工具是核心驱动力**：消融实验表明，Python 沙箱是性能提升的最大来源。其不可替代的价值在于能对评估数据进行**结构化聚合分析**（如类别间混淆矩阵），这是长上下文 LLM 仅通过阅读原始文本数据无法可靠完成的。
*   **自主性与模型质量至关重要**：智能体的自由决策能力（相对于固定循环）和强大的底层优化器模型（GPT-5.4 vs GPT-4o）是取得高性能的必要条件。
*   **代码执行的价值**：通过案例研究展示了代码执行能让优化器**发现结构性规则**（如特定短语导致的标签矛盾），而不仅仅是提炼已有规则。

### 7. 论文优点
*   **创新性强**：首次将“代码即行动”范式系统性地应用于自动提示工程，赋予优化器前所未有的分析和主动权。
*   **解决实际问题**：针对复杂的、多类别的、需要深度结构化分析的 LLM 裁判任务，提供了显著优于以往方法的解决方案。
*   **设计严谨**：通过自动回滚和防护指标下限，成功将一个自由形式的智能体转变为单调改进的优化器，提升了可靠性。
*   **实验扎实**：结合了大规模工业评估和公共基准测试，并通过详尽的消融实验和案例研究，清晰论证了各组件的价值和具体生效场景（发现规则 vs. 修正格式）。
*   **分析深刻**：精准指出了 Python 工具的核心价值在于“结构化聚合”而非单纯的“更多信息”，并通过对比实验（A6消融）证实了这一点。

### 8. 不足与局限
*   **可复现性限制**：核心组件优化器模型（GPT-5.4）是内部部署版本，未公开具体映射关系，外部很难绝对复现。
*   **数据与提示不公开**：工业数据集和优化后的提示内容因包含专有信息而无法公开，影响了对工业实验结果的独立验证。
*   **统计偏差风险**：主表中 `best-of-N` 的搜索池在部分任务上对 SPEAR 不对称，可能高估其性能差距。
*   **验证集接触**：SPEAR 在优化过程中可以看到验证集的聚合标签分布，这与基线方法不公平，可能带来轻微过拟合风险。
*   **应用场景局限**：其架构优势主要体现在结构化错误分析有价值的任务上（如分类混淆），在纯推理和格式匹配任务上优势不明显。
*   **安全模型简易**：沙盒基于信任的优化器模型和安全的数据前提，并非为对抗性攻击设计，直接用于处理不可信的用户数据存在安全隐患。

（完）
