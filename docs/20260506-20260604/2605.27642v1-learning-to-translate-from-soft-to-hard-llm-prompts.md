---
title: Learning to Translate from Soft to Hard LLM Prompts
title_zh: 学习将软提示翻译为硬提示的大型语言模型
authors: "Pitipat Kongsomjit, Suryansh Goyal, Jacob Whitehill"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27642v1"
tags: ["query:prompt-opt"]
score: 9.0
evidence: 将软提示转化为自然语言提示，实现可移植且可解释的提示优化。
tldr: 软提示微调参数高效但可解释性差，限制了透明部署。本文提出训练软提示至自然语言翻译模型，将软提示转为硬提示。定量定性评估表明，该模型生成的硬提示流畅精准，超越InSPEcT等免训练方法。转换所得硬提示可部署于大型闭源API模型，性能优于原软提示，部分任务甚至超越少样本学习，显著推动可解释性与跨模型迁移应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27642v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 360, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2605.27642v1/fig-002.webp\", \"caption\": \"\", \"page\": 7, \"index\": 2, \"width\": 1800, \"height\": 1200}]"
motivation: 软提示微调参数高效但缺乏可解释性，限制了其在透明场景下的应用与信任。
method: 提出软提示至自然语言的专用翻译模型，将连续软提示转为离散文本硬提示。
result: 生成的硬提示在多数据集上流畅精准，效果优于InSPEcT等免训练方法，并成功迁移至大模型，性能超越原软提示甚至少样本学习。
conclusion: 不仅提升了软提示可解释性，还证明翻译硬提示可有效迁移至大模型，性能超越原软提示，为跨模型部署提供新思路。
---

## 摘要
软提示调优是将大型语言模型适配到特定任务的一种参数高效方法，但缺乏可解释性。基于近期对软提示进行解释的研究（Ramati 等人，2024年），我们探索了如何训练一个专用的软提示到自然语言的翻译模型，以获得更高的翻译质量。具体而言，在多个数据集的数据集（DoDs）上的定量和定性比较中，我们证明我们的翻译器能生成流畅、准确的表述，其性能优于现有的免训练方法如 InSPEcT。除了提升可解释性之外，我们的工作还指明了有前景的下游应用：在小型开源模型上优化的软提示可以转化为可移植的文本提示，当部署在较大的闭源API模型上时，其性能超越了原始的软提示，在某些情况下甚至超越了少样本学习。

## Abstract
Soft prompt tuning is a parameter-efficient method for adapting LLMs to specific tasks, but suffers from a lack of interpretability. Building on recent work on interpreting soft prompts (Ramati et al., 2024), we explore how training a dedicated soft prompt to natural language translation model can yield higher translation quality. In particular, in both quantitative and qualitative comparisons on multiple Datasets of Datasets (DoDs), we demonstrate that our translator produces fluent, accurate verbalizations that outperforms existing training-free methods like InSPEcT. In addition to advancing interpretability, our work suggests a promising downstream application: soft prompts optimized on small, open-source models can be translated into portable text prompts that, when deployed on larger closed-API models, exceed the performance of the original soft prompt and, in some cases, even few-shot learning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **核心问题**：软提示调优是一种高效的LLM适配方法，但其本质是连续的、不透明的向量，缺乏可解释性。这使得我们难以理解软提示究竟“学会”了什么，限制了其在透明性要求高的场景下的应用。
*   **研究动机**：现有方法（如将软提示投影到最近的离散token）往往会产生误导性的、矛盾的结果。而先进的免训练解读技术（如InSPEcT）则存在表述不清晰、不可靠等问题。
*   **整体含义**：本文旨在探索一种更强大、更有效的方法，将不透明的软提示“翻译”成可读的自然语言“硬提示”，从而一方面提升其可解释性，另一方面探索将其作为可移植提示在更大模型上重用的可能性，实现“小模型训练，大模型推理”的新范式。

### 2. 论文提出的方法论

*   **核心思想**：训练一个专用的、基于监督学习的序列到序列翻译模型，将连续向量形式的软提示直接“翻译”成对应的自然语言任务描述。
*   **工作流程**：
    1.  **软提示训练**：在一个小型开源LLM上，针对特定任务数据集训练一个软提示。
    2.  **翻译模型训练**：使用相同架构的LLM作为基础，在一个由（软提示，硬提示）对构成的“数据集的数据集”上进行微调，使其学会从软提示生成对应的自然语言任务指令。
    3.  **下游应用（可选）**：将翻译得到的“硬提示”作为输入，提供给一个更大、更强的闭源LLM执行推理。
*   **训练数据构建**：关键在于构建“数据集的数据集”。
    *   **分类任务DoD**：通过提示LLM生成语义上紧密相关的类别标签（如“音乐, 电影, 书籍...”），并为每个类别生成数百个句子，从而构建了5500个分类数据集。其对应的“硬提示”是这些类别标签的列表。
    *   **通用任务DoD**：基于Super-NaturalInstructions数据集构建，包含539个训练任务和96个验证任务，涵盖文本摘要、问答等多种任务。其“硬提示”是任务本身提供的自然语言指令。为了增强数据，还对其指令进行了解释性改写。
*   **翻译模型结构**：在meta-llama/Llama-3.1-8B-Instruct模型上，使用LoRA（一种参数高效微调方法）进行微调，输入为软提示的嵌入向量，输出为自然语言指令文本。

### 3. 实验设计

*   **数据集/场景**：
    *   **自定义DoDs**：用于训练和评估翻译模型的“分类任务DoD”和“通用任务DoD”。
    *   **基准数据集**：使用了InSPEcT论文中评估的5个标准数据集：SST-2、SST-5、AG-News、Subj和TREC。
    *   **分布外泛化测试**：使用了两个真实世界任务数据集，即“虚假评论检测”和“AI生成文本检测”。
*   **评估指标**：
    *   **分类任务**：召回率和F1分数（用于衡量正确类别被提及且无关内容少）。
    *   **通用任务**：ROUGE-L（衡量文本重叠度）和余弦相似度（基于sentence transformer的语义相似度），以及平均百分位秩，衡量任务可区分性。
    *   **下游应用**：将翻译出的提示在GPT-4o-mini上进行推理，与原始软提示在小模型上的表现以及无提示的基线进行比较。
*   **对比方法**：
    *   主要对比对象是免训练的InSPEcT方法。
    *   在下游应用评估中，与少样本学习（Few-Shot Learning）进行了比较。

### 4. 资源与算力

*   **模型基础**：方法基于`Llama-3.1-8B-Instruct`模型。
*   **软提示训练**：使用了PEFT库，以及作者自定义的实现。超参数包括学习率（1e-4到1e-3）、批量大小（8-32）、训练周期等。
*   **翻译模型训练**：使用LoRA（低秩适应）进行微调，LoRA秩为4，并使用了学习率1e-4、批量大小16、权重衰减等。
*   **数据生成**：使用了vLLM库，并调用`Mistral-Small-3.1-24B-Instruct-2503`等更强的模型来生成合成数据。
*   **未明确提及**：**论文未明确指出用于训练的具体GPU型号、数量及总训练时长。**

### 5. 实验数量与充分性

*   **实验数量充足**：实验设计非常全面，包括：
    1.  在多个维度（分类、通用任务、分布外任务）上对翻译模型进行评估。
    2.  与免训练方法InSPEcT在多个数据集和指标上的定量和定性比较。
    3.  训练数据规模影响的消融实验（使用12%至100%的数据）。
    4.  数据增强（指令改写）及其规模影响的消融实验（1倍、10倍、100倍）。
    5.  下游应用验证，包括与软提示基线和少样本学习的比较。
*   **充分性与公平性**：实验设计客观。对比InSPEcT时，为了公平，对其关键的层对超参数在训练集上进行了优化，取最优结果进行比较。实验覆盖了从可解释性评价到实际应用价值的完整链路，论证充分。

### 6. 论文的主要结论与发现

1.  **翻译质量更高**：专用的翻译模型在生成软提示的自然语言描述方面，其流畅性、准确性和一致性均显著优于免训练的InSPEcT方法。
2.  **可解释性强**：即使在软提示本身的任务表现不佳时，翻译模型也能提供与任务相关的、合理的解释，显示出更强的鲁棒性。
3.  **翻译结果具有实用价值**：将翻译出的“硬提示”应用于大型闭源LLM（如GPT-4o-mini）后，其任务性能不仅超越了原始小模型上的软提示，甚至在某些任务上超越了大型LLM自身的少样本学习能力。
4.  **小规模数据即可建立对应关系**：即使翻译模型的训练集较小（例如539个样本），它也能捕捉到软提示与自然语言之间的粗略对应关系，这被比喻为“软提示语言的罗塞塔石碑”。
5.  **存在风格对齐问题**：模型有时会生成过于简洁的翻译，遗漏重要细节，但这属于风格选择，而非理解错误，可以通过预填充文本等方法缓解。

### 7. 优点

*   **创新的方法**：首次提出训练专用的监督模型来翻译软提示，将解释性问题转化为序列到序列的生成任务。
*   **全面的评估体系**：构建了大规模、多样化的“数据集的数据集”，并从多个维度（准确性、相关性、实用性）进行评估，远超以往研究。
*   **连接理论与应用**：不仅解决了可解释性问题，还展示了该方法在实际应用中的巨大潜力，即实现“小模型专训、大模型普用”的可移植提示工作流。
*   **性能优越**：在几乎所有评估中超越了现有最强免训练方法InSPEcT，并展示了在某些困难任务上超越少样本学习的潜力。

### 8. 不足与局限

*   **模型绑定**：翻译模型与训练软提示的基础模型（Llama-3.1-8B-Instruct）深度绑定，若想支持在其他模型上训练的软提示，可能需要重新训练翻译器。
*   **风格失配问题**：翻译结果有时过于简洁，倾向于“流畅”而非“完备”，可能会忽略关键的格式细节或约束，论文承认这是一个有待解决的风格对齐问题。
*   **资源与算力信息缺失**：未报告训练消耗的具体硬件和时间成本，难以评估其实际应用门槛。
*   **翻译准确性的上限**：虽然优于InSPEcT，但在通用任务上，其与真实提示的语义相似度（余弦相似度约0.41）和文本重叠度（ROUGE-L约0.17）仍有较大提升空间，表明很多翻译仍未完全捕捉到任务的全部意图。

（完）
