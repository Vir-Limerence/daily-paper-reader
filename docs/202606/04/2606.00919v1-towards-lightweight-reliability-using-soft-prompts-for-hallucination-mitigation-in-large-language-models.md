---
title: "Towards Lightweight Reliability: Using Soft Prompts for Hallucination Mitigation in Large Language Models"
title_zh: 迈向轻量级可靠性：利用软提示缓解大语言模型中的幻觉
authors: "S M Tahmid Siddiqui, Akib Jawad Ononto, Anoop Singhal, Latifur Khan"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00919v1"
tags: ["query:prompt-opt"]
score: 9.0
evidence: 使用复合损失训练软提示以缓解QA中的幻觉，一种参数高效的提示微调方法。
tldr: 大语言模型在众多领域应用广泛，但其可靠性常因幻觉问题而降低。本文提出参数高效的软提示方法RCSP，通过复合损失训练软提示，以抑制幻觉内容、鼓励不确定性弃权并保持事实召回。在五个生成式QA数据集上，使用Gemma 3和Llama 3.1模型评估，RCSP在F-score上普遍优于标准推理与指令提示基线，且仅需训练少量参数。该工作表明软提示提供了一条模块化且计算高效的途径来提升大语言模型的可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00919v1/fig-001.webp\", \"caption\": \"\", \"page\": 14, \"index\": 1, \"width\": 1000, \"height\": 400}]"
motivation: 大语言模型易产生事实性错误（幻觉），在高风险领域严重损害信任，亟需轻量级缓解方案。
method: 提出责任对比软提示（RCSP），结合对比损失、课程学习与KL正则化训练软提示，以平衡幻觉抑制、弃权与事实召回。
result: 在Gemma 3和Llama 3.1上，RCSP于五个数据集上取得优于基线的F-score，并仅需训练极少量参数。
conclusion: 软提示能以模块化、计算高效的方式显著提升大语言模型可靠性，为幻觉缓解提供新路径。
---

## 摘要
大语言模型已在各个领域被广泛采用，但其可靠性常常因幻觉——听起来合理但事实错误的回答——而受损。在高风险领域中，这些错误会降低信任并带来现实风险。为应对这一挑战，我们提出一种参数高效的方法，使用软提示来缓解生成式问答任务中的幻觉内容并促进负责任的弃权。我们的方法名为负责任对比软提示（RCSP），利用复合损失来训练软提示，以平衡三个目标：抑制幻觉内容、鼓励不确定性下弃权，以及保持或提升事实召回。为实现这些目标，我们将对比损失、课程学习和KL正则化融入训练机制。我们使用以语言模型为评判的框架，在五个多样化的生成式问答数据集上评估了我们的方法。在Gemma 3（12B）和Llama 3.1（8B）主干上的实验结果表明，RCSP有效地平衡了事实召回与幻觉抑制及弃权，相比标准推理和基于指令的提示基线，通常能获得更优的F分数。值得注意的是，这些改进仅需训练其他调优技术所需参数的一小部分。我们的结果表明，软提示为提高大语言模型的可靠性提供了一条模块化且计算高效的路径。

## Abstract
Large language models (LLMs) have seen widespread adoption across various domains, yet their reliability is frequently undermined by hallucinations - responses that are plausible-sounding but factually incorrect. In high-stakes domains, these errors can reduce trust and introduce real-world risk. To address this challenge, we present a parameter-efficient approach that uses soft prompts to mitigate hallucinated content and promote responsible abstention in generative question-answering (QA) tasks.   Our method, called Responsible Contrastive Soft Prompting (RCSP), uses a composite loss to train soft prompts that balance three goals: suppressing hallucinatory content, encouraging abstention under uncertainty, and preserving or improving factual recall. To achieve these goals, we incorporate contrastive loss, curriculum learning, and KL regularization into our training mechanism. We evaluate our approach on five diverse generative QA datasets using an LLM-as-a-Judge framework. Experimental results on the Gemma 3 (12B) and Llama 3.1 (8B) backbones demonstrate that RCSP effectively balances factual recall with hallucination suppression and abstention, yielding a generally superior F-score over standard reasoning and instruction-based prompting baselines. Notably, these improvements are achieved by training only a fraction of the parameters required by other tuning techniques. Our results demonstrate that soft prompts provide a modular and computationally efficient path toward improving LLM reliability.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 研究动机与核心问题
- **核心问题**：大语言模型（LLM）在生成式问答中会产生“幻觉”（听起来合理但事实错误的回答），在网络安全、医疗等高可靠性场景中会引发严重风险。
- **研究缺口**：现有缓解方法存在局限：全参数微调（如R-Tuning）计算成本极高；提示工程（如情感提示、推理提示）效果不稳定且无法精细调控；检索增强（RAG）依赖外部知识库，不改变模型内部生成行为；推理时干预不能塑造模型的学习目标。更重要的是，现有评估基准通常将“弃权”视为错误，变相鼓励模型猜测。
- **整体含义**：论文旨在提出一种**轻量级、参数高效、模块化**的方法，在不修改主干模型参数的前提下，同时实现**抑制幻觉、鼓励负责任弃权、保持事实召回**这三个目标，从而系统性地提升LLM的可靠性。

## 2. 方法论：负责任对比软提示 （RCSP）
### 2.1 核心思想
- 在冻结的LLM输入层之前添加一组**可训练的连续嵌入向量（软提示）**，仅优化这极少量的参数（例如Gemma 3 12B上仅0.009%参数），以“贴片”方式引导模型行为。
- 训练分为**离线训练**（利用复合损失学习软提示并用评判模型选出最佳提示）和**部署推理**（将选定的软提示置于用户问题前生成回答）两个阶段。

### 2.2 复合损失函数设计
总损失为对比损失与效用损失之和：
\[
\mathcal{L} = \mathcal{L}_{\text{contrast}} + \mathcal{L}_{\text{utility}}
\]

- **对比损失**（\(\mathcal{L}_{\text{contrast}}\)）：
  作用于对比数据集（三元组：问题 \(q\)、幻觉答案 \(h\)、事实答案 \(f\)）。公式为：
  \[
  \mathcal{L}_{\text{contrast}} = (1-\mu) \text{CE}(a) + \mu \text{CE}(f) - \lambda \text{CE}(h) + \beta_{\text{contrast}} \text{KL}(p_\theta \| p_{\text{ref}})
  \]
  其中 \(a\) 为弃权目标（如“我不知道”）。
  - **课程学习**：\(\mu\) 从 \(\mu_{\text{start}}\)（初期侧重弃权）线性增长至 \(\mu_{\text{end}}\)（后期侧重事实答案），避免模型过度弃权。
  - **负交叉熵项**（\(-\lambda \text{CE}(h)\)）：惩罚模型对齐幻觉答案，作为对比性反监督信号。
  - **KL正则化**：约束当前模型分布 \(p_\theta\) 不偏离基础模型分布 \(p_{\text{ref}}\)，防止行为剧烈变化。

- **效用损失**（\(\mathcal{L}_{\text{utility}}\)）：
  作用于效用数据集（问题-事实答案对），保持常规问答能力：
  \[
  \mathcal{L}_{\text{utility}} = \text{CE}(f) + \beta_{\text{utility}} \text{KL}(p_\theta \| p_{\text{ref}})
  \]
  仅包含正向交叉熵和KL正则化，不涉及反监督或课程机制。

所有交叉熵损失仅在答案token上计算，并按长度归一化，确保公平。

## 3. 实验设计
### 3.1 数据集
- **训练集**：
  - 对比数据集：HaluEval前8,000题 + TruthfulQA前600题（用于对比损失）。
  - 效用数据集：TriviaQA的27,000事实问答对（用于效用损失）。
- **评估数据集**（各取≤500题固定子集，TruthfulQA为217题）：
  - HaluEval（测试集）
  - TruthfulQA（测试集）
  - SimpleQA（前沿模型压力测试）
  - MedHallu（医学幻觉基准）
  - WebQuestions（开放域问答）

### 3.2 评估方式
- 采用**以LLM为评判（GPT-4.1）**的协议，生成回答后由判官标注为三类：
  - **FACT**：事实正确且与参考答案一致；
  - **HALL**：与参考答案矛盾或包含无依据主张；
  - **ABS**：明确弃权（如“我不知道”），且不含矛盾信息。
- 判决规则严格：将离题或仅重复问题的回答视为幻觉而非弃权。
- 综合指标为 **F-score**，即整体准确率 \(A\) 与条件准确率 \(C\)（仅尝试回答中的正确率）的调和均值。
- 生成阶段使用确定性解码（temperature=0，top-p=1，beam size=1）。

### 3.3 对比方法
- **Base**：原始预训练LLM（Gemma 3 12B、Llama 3.1 8B）。
- **Append-idk**：在问题后附加英文指令“若不知道答案请回复‘我不知道’”。
- **EmoPrompt**：从心理学理论导出的11种情感提示，报告其平均表现。
- **Plan-and-Solve**：基于推理的提示，要求模型规划并逐步解决。
- **RCSP**（本文方法）：约40-128个token的软提示，在输入层训练。

## 4. 资源与算力
- 文中提到根据可用性和内存需求使用 **L4、A100 或 H100 GPU**，但未明确记录具体GPU数量、单次训练耗时或总算力消耗（如GPU小时）。
- 训练超参数（见表5）：学习率 \(3\times10^{-5}\)，batch size 32，Gemma 3 12B训练3个epoch，Llama 3.1 8B训练4个epoch，权重衰减0.01等。基于这些信息可推断训练开销较低，但未做详细计算和汇报。

## 5. 实验数量与充分性
- **主要实验**：在2个不同模型族（Gemma 3 12B、Llama 3.1 8B）× 5个评估数据集 × 5种方法（含RCSP和4个基线）上进行了全面的生成与评判，共计约50组配置对比。
- **消融实验**：在Gemma 3 12B + TruthfulQA上进行了5种消融变体（移除课程学习、移除KL正则、移除 \(L_{\text{utility}}\)、移除 \(L_{\text{contrast}}\)、固定 \(\mu\)），并分析了软提示长度（10-160 tokens）和嵌入维度（32-512）的敏感性。
- **充分性与公平性**：
  - 对比方法均属于轻量级提示类干预，控制变量合理。
  - 所有方法使用相同的解码设置和评估协议，客观性较强。
  - 评估基准多样，涵盖通用、对抗、医学等领域，提升了结论的泛化性。
  - 不足之处在于未与重量级微调方法（如R-Tuning、QLoRA）直接对比绝对性能（仅参数效率上做了对比），且LLM评判本身可能引入偏见，F-score的聚合方式可能掩盖不同的弃权-回答行为模式。

## 6. 主要结论与发现
- **回答研究问题**：
  - RQ1（能否促进弃权、降低幻觉而不修改参数）：是。RCSP在多数数据集上降低了幻觉率，适度引入弃权。
  - RQ2（能否不损害事实召回）：是。RCSP在大多数数据集上保持或提高了FACT比例，F-score普遍最优。
  - RQ3（能否提供计算高效、模块化方案）：是。RCSP仅训练约0.009%参数，实现轻量级“即插即用”。
- **具体结果**：
  - 在Gemma 3 12B上，除MedHallu外所有数据集RCSP均获得最佳F-score；Llama 3.1 8B上，除MedHallu外也最优。
  - SimpleQA上所有方法幻觉率均超80%，但RCSP仍达到最高F-score和事实率。
  - 消融实验证明各组件（课程学习、KL正则、对比损失、效用损失）均对性能有正面贡献，移除任一组件均导致幻觉上升或事实下降。
  - 软提示长度和嵌入维度有最优值，过大反而可能降低性能，佐证其轻量性。

## 7. 优点
- **轻量高效**：仅训练输入嵌入层的极少量参数（约百万级），远低于QLoRA等参数高效微调方法。
- **模块化设计**：软提示独立于模型权重，可即插即用、灵活替换，适合安全敏感场景的动态路由。
- **复合损失设计精巧**：对比反监督、课程学习与KL正则化的有机结合，能够精细调控弃权与事实回答的平衡。
- **评估严谨**：采用LLM-as-a-Judge的严格三级标注，并用F-score综合衡量，避免单一准确率或幻觉率的片面性。
- **消融与敏感性分析全面**：验证了各组件的有效性和超参数影响。

## 8. 不足与局限
- **评判偏差风险**：LLM评判可能自带偏见，尽管使用了固定模板和确定性解码，但在复杂开放式问答中仍可能误判。
- **指标单一聚合**：F-score隐藏了不同的行为组合（大量弃权高条件准确率 vs. 少量弃权中等准确率可获相同分数），无法区分“谨慎”与“冒险”型模型。
- **领域覆盖有限**：Llama 3.1在MedHallu上表现不及基线，显示方法对特定领域（医疗）的跨架构迁移尚有不足；评估仅限于生成式短文本QA，未涉及长篇、对话或多模态任务。
- **训练数据依赖**：对比训练依赖HaluEval和TruthfulQA的对抗样本，对新出现的幻觉模式泛化性未验证。
- **未与重量级方法直接对比**：仅作为轻量级方案与提示基线比较，缺乏与微调类幻觉缓解方法在相同任务上的直接性能对比。
- **算力与效率报告不完整**：没有详细记录训练时间、显存占用或能耗，难以完整评估其实际部署中的成本效益。

（完）
