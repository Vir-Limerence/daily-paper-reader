---
title: "DD-GEPA: Prompt Optimization for Dialogue Disentanglement Focusing on Task Instruction and Utterance Representation"
title_zh: DD-GEPA：面向任务指令和话语表示的对话解耦提示优化
authors: "Naoki Takada, Tatsunori Mori"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07894v1"
tags: ["query:prompt-opt"]
score: 10.0
evidence: 使用GEPA自动优化对话解缠的提示组件，分解并优化提示
tldr: 群聊中的交错对话给对话理解带来挑战，大语言模型虽可用于对话解缠但准确率不高。本文提出自动提示优化方法DD-GEPA，将提示分解为任务指令、语句表示和输出指令三个组件，并采用GEPA框架联合优化。在基准数据集上，优化后的提示显著提升了解缠准确性，超越了手工设计提示，表明自动化提示优化能增强LLM对复杂对话的处理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07894v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 779, \"height\": 755, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07894v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1657, \"height\": 674, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07894v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 772, \"height\": 417, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.07894v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 806, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07894v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1664, \"height\": 469, \"label\": \"Table\"}]"
motivation: 多参与者的群聊常导致对话交错，大语言模型虽具潜力但对话解缠准确率仍低，需改进提示方法。
method: 将提示分解为任务指令、语句表示和输出指令，用GEPA优化方法自动优化这三部分。
result: 在基准数据集上，优化提示显著提高对话解缠准确率，并超越手工设计的提示。
conclusion: 提出DD-GEPA自动提示优化框架，有效提升LLM对话解缠性能，为复杂对话处理提供新思路。
---

## 摘要
多方聊天通常包含交织的对话，因为多个参与者可能同时讨论不同话题。对话解耦通过将纠缠的话语序列分离为连贯的对话来解决这一问题。虽然大型语言模型在这一任务上展现出潜力，但它们在对话解耦方面仍面临困难，准确率较低。本文提出了一种基于大型语言模型的对话解耦自动提示优化方法。我们将提示分解为三个部分：任务指令、话语表示和输出指令，并使用化合物人工智能系统的优化方法GEPA对其进行优化。在基准数据集上的实验表明，优化后的提示相比原始提示提高了对话解耦的准确率，并可能超越人工设计的提示。

## Abstract
Multi-party chat often contains interleaved dialogues because multiple participants can discuss different topics at the same time. Dialogue disentanglement addresses this problem by separating an entangled utterance sequence into coherent dialogues. While large language models (LLMs) are promising for this task, they still struggle with dialogue disentanglement and achieve low accuracy. This paper proposes an automatic prompt optimization for LLM based dialogue disentanglement. We decompose the prompt into three components: task instruction, utterance representation, and output instruction, and optimize them using GEPA, an optimization method for compound AI systems. Experiments on benchmark datasets show that the optimized prompts improve dialogue disentanglement accuracy over the original prompts and can surpass hand crafted prompts.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：在多方聊天（如 Slack、IRC）中，多个话题常常并行推进，导致话语序列纠缠在一起（`interleaved dialogues`）。对话解耦（`dialogue disentanglement`）旨在将这种混杂的对话流拆分成若干条连贯的对话线索，是下游对话分析任务的重要预处理步骤。
- **核心问题**：大型语言模型虽然具备上下文推理能力，但直接用于对话解耦时准确率较低。现有手工设计的提示对 LLM 的表现高度敏感，且因任务本身判断标准难以清晰定义（人工标注一致性也不高），手工优化提示成本高、效果不稳定。因此，如何自动地为 LLM 生成高效的对话解耦提示，成为一个关键挑战。
- **整体含义**：本文提出自动提示优化框架 **DD‑GEPA**，通过将提示结构分解成三个可独立优化的模块，利用进化式搜索和错误反思来自动寻找更优的提示组合，从而提升 LLM 在对话解耦上的准确率。

### 2. 论文提出的方法论
- **核心思想**：将对话解耦提示分解为**任务指令**、**话语表示**和**输出指令**三个独立模块，然后对这三个模块进行联合自动优化。
- **模块定义**：
  - **任务指令 (`task instruction`)**：定义对话解耦任务、说明判断回复关系的证据和方法。
  - **话语表示 (`utterance representation`)**：规定如何将时间戳、发言人 ID、消息内容等信息格式化为 LLM 可读形式（如普通文本、JSON 等）。
  - **输出指令 (`output instruction`)**：指定模型输出预测结果的格式（如仅返回父话语编号，或返回 JSON 带 `is_new_dialogue` 字段等）。
- **优化算法**：
  - 基于 GEPA（`GEPA: Reflective prompt evolution can outperform reinforcement learning`）并针对对话解耦进行扩展，形成 **DD‑GEPA**。
  - **程序与模块更新**：维护一个候选`程序`池，每个程序包含三个模块。采用 Pareto‑based 选择挑出程序，轮询决定本轮要更新的模块。对选定的模块，采样 3 个训练实例作为 `minibatch`，收集 LLM 执行轨迹（输入、输出、推理文本、正确性），对错误案例额外生成失败解释。然后调用 LLM 生成该模块的新版本，与另两个未变模块组合成新程序。若在 minibatch 上的正确率提升，则加入候选池，并在 300 条验证实例上评估得分。
  - **外部功能扩展**：由于话语表示和输出指令的变更会改变输入/输出格式，DD‑GEPA 增加了两个自动生成代码的外部函数——根据提议的表示格式生成话语序列转换代码，以及根据新的输出指令生成预测结果解析代码，从而保证评估过程可自动化。
  - **数据集构建**：从 IRC 开发集中挑选较难但仍可被 LLM 解决的 450 条实例（排除所有方法都失败的 63 条），按难度平衡划分出 150 条训练和 300 条验证，用于优化过程。
- **流程示意**：论文的 Figure 2 展示了从候选池选择程序 → 轮选模块 → 采样轨迹 → 生成新模块 → 验证并更新的循环。

### 3. 实验设计
- **数据集**：使用标准 benchmark **Ubuntu IRC 数据集**（Kummerfeld et al., 2019），包含真实的多方技术支持聊天记录。
  - 优化用 `development` 集（共 2500 条话语，其中 450 条被抽作优化数据集）。
  - 最终测试在 `test` 集（5000 条话语）上进行。
- **对比方法**：
  - 非 LLM 方法：Elsner (2008)、DiaBERT (Li et al., 2020)、Struct (Ma et al., 2021)、DiHRL (Li et al., 2025)。
  - 专有 LLM 方法：GPT‑4.1 和 Gemini 2.5 pro 的 DLA+SC 版本（Takada & Mori, 2026）。
  - 本文自身对比的四种提示设置：`Seed 1`（简单指令＋纯文本表示＋单一数字输出）、`Seed 2`（同 Seed 1 但改用双键 JSON 输出）、`Baseline`（手工设计的最强提示：Seed 1 指令＋JSON 话语表示＋双键输出）、`Optimum`（DD‑GEPA 优化所得）。
- **评估指标**：
  - 整体聚类质量：`VI`（越低越好）、`ARI`、`NMI`、`1‑1 accuracy`、`S‑F1`。
  - 局部聚类质量：`Local 3`（窗口大小为 3）。
  - 精确对话匹配：精确率、召回率、F1。
- **LLM 设置**：主要实验用 `Qwen3‑30B‑A3B‑Thinking‑2507`，回复预测时温度=0；优化生成新提示时温度=0.6。候选父话语限制为最近的 50 条。

### 4. 资源与算力
- 论文中**未明确说明**所使用的 GPU 型号、数量以及优化过程的实际训练时长。
- 仅提及使用 Qwen3‑30B（开放权重的 30B 参数模型）进行所有主要实验，并用 GPT‑5.2 作为对比来衡量基线性能，但未提供硬件配置或计算开销细节。

### 5. 实验数量与充分性
- **实验规模**：
  - 在 IRC 测试集上进行了 4 种提示设置的完整评估，并与 7 个外部方法（含非 LLM 和专有 LLM 方法）进行了对比。
  - 优化过程中记录了 33 个生成程序的验证准确率变化曲线（Figure 3），展示了优化动态。
- **消融与分析**：
  - 虽然没有对三个模块进行严格的消融实验，但通过 Seed 1 / Seed 2 / Baseline 的设定间接反映了话语表示和输出指令各自的影响。
  - 对 Optimum 的错误进行了五类模式分析。
- **充分性与公平性**：
  - 优化与评估采用标准数据集划分，未在优化过程中使用测试集，保证了公平性。
  - 所选的对比方法覆盖了主流基线，且 LLM 端均使用同一基座模型，比较客观。
  - 不足之处在于缺少跨领域验证，且优化过程只运行了有限轮次（提前停止），可能未完全探索提示空间。

### 6. 论文的主要结论与发现
- **自动优化有效**：DD‑GEPA 能够成功优化对话解耦提示，在 Qwen3‑30B 上，`Optimum` 在所有评估指标上均超越手工设计的 `Baseline`。
- **性能提升幅度**：与初始种子提示相比，Optimum 显著缩小了与专有 LLM 的差距，但仍低于传统非 LLM 方法 DiHRL，且不及采用更强专有 LLM 的 DLA+SC 方法。
- **优化瓶颈**：优化在早期迭代即达到平台期（5 次更新后验证准确率不再增长），原因在于训练实例大多已被解决，且有限的 minibatch 轨迹难以覆盖高度多样的多人对话模式。
- **错误模式**：主要错误集中在简短问候/反应的分割、系统消息的误关联、领域专有名词的理解偏差、隐含收件人判断以及上下文信息不足等情况。

### 7. 优点
- **提示分解策略**：将复杂提示拆解为三个可独立优化的语义模块，使优化目标更具结构性，也便于未来扩展。
- **方法适配与扩展**：在 GEPA 基础上，通过自动生成格式转换代码，成功处理了话语表示和输出指令这类“非自然语言”组件的优化，拓宽了 GEPA 的应用范围。
- **数据集构造**：专门构建了面向提示优化的高难度、可判定的子数据集，避免了简单实例主导的虚假提升。
- **分析深度**：不仅提供了多项聚类指标，还进行了错误类型分析，揭示了当前 LLM 对话解耦的能力边界。
- **可解释性与可迁移性**：优化得到的提示全部为自然语言形式，保留了可读性，方便人工检查和在不同模型间迁移。

### 8. 不足与局限
- **优化提前收敛**：优化过程很快停滞，更大的候选池、更多样的训练轨迹或更难的实例或许能带来进一步提升，但论文未探索这些方向。
- **领域单一与数据泄露风险**：仅在 Ubuntu IRC 数据集（编程技术聊天）上实验，方法对其他领域（如游戏、闲聊）的泛化能力未经验证；同时数据集是公开的，可能已被 LLM 预训练所涵盖，存在数据泄露的潜在偏差。
- **长程依赖限制**：回复关系只考虑前 50 条话语，无法处理更长距离的跨对话关联。
- **仅针对基座框架**：未将此前提出的 `Dialogue‑Level Assignment (DLA)` 和 `Subsequent Context (SC)` 扩展纳入优化范围，因此优化结果不代表 LLM 对话解耦的最强形态。
- **缺少消融实验**：未定量分析单独优化每个模块（任务指令、表示、输出）的贡献，难以判断哪一部分改进最为关键。
- **算力信息缺失**：未报告优化过程所需的 GPU 资源与时间成本，降低了可复现性。

（完）
